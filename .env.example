# .env.example
# Copy this file to .env and replace the values with your own secure passwords

DB_USER=admin
DB_PASSWORD=replace_this_password
DB_NAME=support_ticket_management_db
DB_PORT=5432

DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@localhost:${DB_PORT}/${DB_NAME}